#!/bin/bash

# 不能以'/'结尾
GIT_REPO_PATH=/Users/swordess/project/example-project

# ossutil需要的配置文件
OSS_CONFIG=/Users/swordess/.aliyun-oss-tool/example.config

# 上传到OSS的路径位置, 即目录位置, 不以'/'结尾
OSS_BUCKET_PATH=oss://swordess-example
